# requirements.txt — 仅供 GitHub Actions 构建使用
# 本地开发用 pyproject.toml + uv 即可；这里只把打包需要的运行时依赖固定下来，
# 留弹性范围（>=），避免某个补丁版被 yank 导致 CI 红
PyQt6>=6.7
python-docx>=1.1
lxml>=5.0
pyinstaller>=6.10
# Pillow 只在打包时用：PyInstaller 用它把 1024x1024 的 app_icon.png 缩到 splash 限制 760x480
# 通过 build.py 里的 --exclude-module PIL，运行时 exe 不带 Pillow，不影响体积
Pillow>=10.0

